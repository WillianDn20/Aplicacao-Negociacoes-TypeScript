export enum DiasDaSemana { //Por padrao o enum ja defini o valor do elemento a partir de 0, então domingo = 0, segunda =1, etc... //Aqui declaramos explicitamente o valor de cada, para que caso a ordem seja alterada, o resultado para a função vai ser o mesmo.
    Domingo = 0,
    Segunda = 1,
    Terca = 2,
    Quarta = 3,
    Quinta = 4,
    Sexta = 5,
    Sabado = 6
}